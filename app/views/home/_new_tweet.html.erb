<%=form_with(model: @tweet, local: true) do |f|%>
        <p>
            <%=f.label :message%>
            <br>
            <%=f.text_area :message%>
        </p>
        <button type="submit">Tweet</button>
<%end%>

